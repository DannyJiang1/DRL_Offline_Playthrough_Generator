Use Python 3.12 for the environment.

Train model to create checkpoints of the model itself.

Use Generate Playthroughs to generate actual playthroughs/trajectories (right now generates 500 trajectories per model checkpoint).
